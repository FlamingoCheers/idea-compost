# Idea Compost 长期工作计划（自主工作状态文件）

> 本文件是持久化的任务状态。任何一次会话中断后，恢复工作的入口就是这里。
> 规则：每完成一项，把 [ ] 改为 [x] 并附一行结果；发现新问题写入 BUGFIX-LOG.md。

## 当前状态快照（2026-08-28）

- 代码版本：M4 已推送（7df1b79 生态引擎 + 7a3524d 决策日志）
- 测试版本：0.2.0 / versionCode 2（已加 release 签名配置）
- 全盘审计：已完成（§2 全对齐；BUG-009~013 已修复；36/36 回归 + 发布版烟测 9/9）
- 发布版：v0.2.0 收尾（演示模式删除/导入导出/测试连接/等待页/卡片等高 已实施+烟测）
- 已确认 bug：见 docs/质量/BUGFIX-LOG.md

## 1. 用户总需求（本轮，m00846）

> 全面过一遍整个项目：1) 检查每处接口与设计是否对应；2) 检查功能能否落实；
> 3) 漏洞汇总成一个文件；4) 固化修补记录与工作计划；5) 逐步完成。
> 另：修完跑全部测试；截图内容与命名一致性核查；长期任务持久化；整体性修复；git commit。

### 1.1 发布版需求（m01018，13 项全部实施完成）

> 收款码（assets 兜底+相册选图）/ 删演示模式 / 去「图片」chip / 瀑布流等高卡片+badge 单行 /
> 融合增殖确认答复 / 慢因答复（16-25 次串行调用，同轮并行列 v0.3）/ 导入导出 zip /
> 等待页冥想句 / 铲子触肥 / 屏幕常亮 / 暂停文案改诚实 / 测试连接按钮 / 确认后按序实施→发布版。
> 烟测 9/9 通过（见 BUGFIX-LOG 结论）；前台服务熄屏续跑列 v0.3。

## 2. 审计清单（静态：代码 vs 设计）

| # | 范围 | 对照物 | 状态 |
|---|------|--------|------|
| A1 | 数据层 Entity/DAO/DDL | specs/01-数据模型与DDL.md | [x] 对齐（DB v3；实体列与实测库 schema 一致；idea/compost/agent/probiotic/bed_event/llm_call 六表） |
| A2 | 堆肥七阶段管线 | specs/03-思想园丁调度协议.md + prompts/gardener/* | [x] 对齐（S1-S5 键序 identify/convoke/r1-r3/integrate/assess 与 stages.md 卡键一一对应；stage 复用支持断点续跑；营养分摊在 S5） |
| A3 | 生态引擎（活力/压缩/休眠/唤醒/增殖/融合） | specs/02-Agent生态协议.md | [x] 对齐（V 公式 45/30/25 权重、压缩 V<15&d>90、休眠 d>180、唤醒近 14 天渣命中≥2、增殖 pc≥5&V≥60、融合共现≥4；四类变换 M4 实测；融合建议路径未实测=遗留） |
| A4 | 益生菌系统 | specs/04-思想益生菌系统.md | [x] 对齐（6 内置+自定义 CRUD+隐藏内置+boosts 权重注入 roster；M4 实测） |
| A5 | 堆肥输出十节结构 | specs/05-堆肥输出协议.md + OutputScreen | [x] 对齐（十节标题与 spec §29 一致；动态断言通过；保存为面包渣+四键反馈接线正确） |
| A6 | AI 接入（直连/Mock/遥测） | specs/06-AI接入与多供应商.md | [x] 对齐（BYO Key 加密存储+供应商直连+stageKey 遥测落库；真实 API 429 错误面已在 M3 实测） |
| A7 | UI 全屏幕接线（每个可点元素→VM→数据） | 08-视觉与工程 + 各 Screen.kt | [x] 对齐（全量接线图建立：7 屏 40+ 交互点全部落到 VM/导航；无死按钮；幽灵芯片=设计内占位「即将上线」） |
| A8 | 导航/DI/Manifest/构建配置 | AppNavHost + MainActivity + gradle | [x] 对齐（7 路由闭环；onboarded 门控一致；EncryptedSharedPreferences 单例+损坏重置+明文迁移；INTERNET 权限齐备） |
| A9 | Prompt 卡资产完整性 | prompts/ vs assets/prompts/ | [x] 对齐（17 agent 卡 7+6+4、6 益生菌卡、S1-S5+3 轮调度卡全部可解析且与引擎键匹配） |

## 3. 动态验证清单

| # | 场景 | 验证方式 | 状态 |
|---|------|---------|------|
| D1 | 首启选菌域→进入首页 | uiautomator dump 断言 | [x] 通过 |
| D2 | 记 2 条面包渣 | 卡片可见断言 | [x] 通过 |
| D3 | 多选→设置→益生菌→开始堆肥 | 各步断言 | [x] 通过 |
| D4 | 等待页→产物页（十节） | 断言 9/9 节标题 | [x] 通过 |
| D5 | 反馈按钮 | 点击成功 | [x] 通过 |
| D6 | 个人页 6 区块渲染 | 断言 6/6 | [x] 通过 |
| D7 | 头像循环 | profile.xml 变化 | [x] 通过（BUG-002 定案：App 无 bug，测试脚本正则/坐标缺陷） |
| D8 | 昵称编辑保存 | profile.xml 断言 | [x] 通过（BUG-001 定案：App 无 bug，测试脚本 maxY/ESC/聚焦缺陷） |
| D9 | 夜间任务+夜报 | 事件断言 | [x] 通过（此前 M4 实测） |
| D10 | 捐赠对话框 | 开/关 | [x] 通过 |
| D11 | 自定义益生菌增删改 | full_test P3-P5 | [x] 通过（run15/16：新建→入网格→编辑→删除落库，重试校验） |
| D12 | 堆肥列表页渲染 | full_test P10 | [x] 通过（已成熟卡+菌群清单断言，截图 11） |
| D13 | 真实 API 模式（非 mock） | 需要 Key，冒烟即可 | [x] M3 已实测通过（真实 API 全链路 + 429 错误面）；长期冒烟列入 v0.2 守则 |
| D14 | 截图命名↔内容一致性 | 重拍并逐张断言 | [x] 通过（run16 产 01-14 共 14 张，逐张人核名实相符；BUG-005 关闭） |

## 4. 收尾清单

- [x] 全部 P0/P1 bug 修复（App 真 bug 009/010/011/012 全修，BUGFIX-LOG 终版结论）
- [x] assembleRelease 打包（BUILD OK，签名 keystore 已配）
- [x] git commit（分阶段；静态审计 92003bb + 本轮修复与终验）
- [x] 最终报告（审计结论 + 测试结果 + 产物路径，见会话报告与 BUGFIX-LOG 结论节）

## 5. v0.3 发布计划（m01288，2026-08-29）

> 用户真机实测反馈：中度堆肥约 20 分钟（时长提示不准）；冲突节排版差；粘贴 chip 未实现；badge 截断；捐赠码应只读可保存。规划确认：并行并发度=4，失败重试 2 次后跳过。

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| V1 | 并行堆肥 | S3 同轮 Agent 调用并发（Semaphore=4）；失败重试 2 次（指数退避）后跳过该菌、本轮继续；stage 完成即落库（保断点续跑）；等待页进度细化到「第n轮 x/y 菌」 | [x] |
| V1b | 并行验证 | 本地假 OpenAI 服务 + adb reverse 跑通全流程；服务端日志验证并发重叠与重试行为 | [x] |
| V2 | 前台服务 | 发酵期间 Foreground Service + 常驻通知；熄屏/离开页面发酵继续（并行改造后等待页文案同步更新） | [x] |
| V3 | 时长提示移除 | SetupScreen 只留轮数（浅2/标准3/深4），删除时长估计（中度实测 20 分钟与提示不符） | [x] |
| V4 | 冲突节排版重做（P1） | 废弃横向思维导图式布局；改纵向冲突卡列表（两侧观点上下排列+⚡标记）；适配各尺寸屏幕；清理 idea:N 标记泄漏 | [x] |
| V5 | 输入页三修（P3） | ①去掉「粘贴」chip ②「未发酵」badge 独占底行防截断 ③日期移至卡片最上方 | [x] |
| V6 | 捐赠码只读化（P2） | 移除「放入收款码图片」；改「保存收款码图片」（MediaStore 存相册，实测落盘）；用户不可更换作者的码 | [x] |
| V7 | v0.3 发布 | versionName 0.3.0 / versionCode 3；BUGFIX-LOG 补录；回归+烟测；release APK 交付 | [ ] |

遗留备注：
- BUG-007（destructive migration）：v0.3 无 schema 变更（DB 仍 v3），无需 Migration；未来任何升版必须逐版编写
- 同轮辩论（Agent 可见同轮伙伴发言）属未来可选阶段类型，天然串行，不套用 V1 并行规则（specs/41 备注）
- 前台服务与 V1 改造后，等待页「离开后发酵会暂停」文案需再次核对（若 V2 落地则改为「离开也会继续发酵」）

## 6. 长期自主工作守则

1. 断点恢复：读本文件 §2/§3 未勾选项 → 接着做。
2. 每完成一项立即勾选并 commit（工作日志与代码分开提交）。
3. 新发现 bug 立即登记 BUGFIX-LOG.md（先记录后修复，防丢失）。
4. 修复原则：先读周围代码与规格，再动手；一处改动必须核对所有调用方。
5. 截图纪律：只在断言 PASS 之后拍摄，命名=断言的场景。
6. 测试脚本放 %TEMP%\opencode\，不进仓库；产物截图进 design/。
